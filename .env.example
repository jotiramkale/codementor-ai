# These belong to the future FastAPI backend (Phase 19+), not the React app.
# A Vite/browser app must never hold a real API key — see src/services/aiService.js
# for why. Kept here now so the variable name isn't lost/reinvented later.

GROQ_API_KEY=your_groq_api_key_here
