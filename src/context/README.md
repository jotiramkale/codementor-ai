React Context providers for cross-cutting state. Currently holds
AuthContext.jsx (added Phase 3) — mock sign-in/sign-up/sign-out state,
consumed via the useAuth() hook. A theme context may be added later if
needed.
