Local mock/static data. Currently holds `navigation.js` (shared
sidebar/drawer nav config, Phase 2), `dashboardMockData.js` (fabricated
Dashboard stats/activity, Phase 4), `problemsMockData.js` (the demo
problem catalog + starter code, Phases 5-7), and `languages.js` (shared
editor language list, Phase 9 — used by both CodeEditor and the
submission history table). All of it is fabricated — none comes from a
real account or database yet.
