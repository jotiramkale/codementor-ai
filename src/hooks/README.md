Custom React hooks (e.g. useAuth, useTimer, useSubmission) that wrap
state logic shared across components. Empty for now — first hooks
likely arrive with Phase 3 (auth state) and Phase 9 (solving timer).
