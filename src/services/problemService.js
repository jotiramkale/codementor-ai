/**
 * problemService.js
 * Not used yet — scaffolded ahead of Phase 5 (DSA problem listing).
 * Will fetch problems, filters, and per-problem detail from FastAPI + PostgreSQL.
 */

export async function listProblems(/* { difficulty, topic, status } */) {
  return []
}

export async function getProblemById(/* id */) {
  return null
}
