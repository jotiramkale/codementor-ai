/**
 * userService.js
 * Not used yet — scaffolded ahead of Phase 21 (User Profile) and the
 * user-memory phases. Will fetch profile info and AI-observed learning
 * patterns, scoped strictly to the logged-in user_id.
 */

export async function getProfile(/* userId */) {
  return null
}

export async function getMemory(/* userId */) {
  return { patterns: [] }
}
